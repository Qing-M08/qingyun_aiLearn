# 青云智学后端开发进度文档

> 最后更新：2026-07-16  
> 项目版本：0.5.0  
> 文档版本：1.4

---

## 一、项目概述

**青云智学**是一个 AI 驱动的全流程学习辅助软件后端系统，采用 FastAPI + SQLAlchemy + Celery + PostgreSQL 技术栈，提供智能学习路线生成、苏格拉底式答疑、间隔重复复习等核心功能。

### 1.1 技术栈

| 组件 | 技术选型 | 版本 |
|------|---------|------|
| Web 框架 | FastAPI | ≥0.115 |
| ORM | SQLAlchemy | ≥2.0 (async) |
| 数据库 | PostgreSQL | 16 |
| 图数据库扩展 | Apache AGE | - |
| 向量检索 | pgvector | ≥0.3 |
| 缓存/消息队列 | Redis | 7 |
| 异步任务 | Celery | ≥5.4 |
| 搜索引擎 | Meilisearch | v1.8 |
| LLM 服务 | DeepSeek API | OpenAI 兼容接口 |
| 嵌入模型 | sentence-transformers | bge-m3 |
| 认证 | JWT | python-jose |
| 日志 | structlog | ≥24.1 |

### 1.2 基础设施

- **容器化**：Docker Compose（PostgreSQL + Redis + Meilisearch）
- **数据库迁移**：Alembic
- **代码规范**：Ruff + MyPy
- **Python 版本**：3.11+

---

## 二、系统架构

### 2.1 项目结构

```
backend/
├── alembic/                    # 数据库迁移
│   ├── env.py
│   └── versions/
│       ├── 5fe73777a71e_initial_schema.py
│       ├── ff551d88ddc7_initial_schema.py
│       ├── c0826ebcce52_add_note_id_to_lectures.py  # 讲义关联笔记
│       ├── 20260710_add_agent_tables.py             # Agent 会话+消息表
│       ├── 20260711_change_embedding_dim_to_512.py  # 嵌入维度调整
│       ├── 20260711_add_note_organize_fields.py     # 笔记 AI 整理溯源字段
│       └── 20260715_add_agent_visibility.py          # Agent 会话 visibility 字段（Sprint 10）
├── app/
│   ├── agent/                  # Agent 模块（Phase 3）
│   │   └── prompts/
│   │       └── agent.py        # Agent 系统 Prompt
│   ├── ai/                     # AI/LLM 核心
│   │   ├── prompts/           # Prompt 模板
│   │   │   ├── route.py       # 学习路线生成
│   │   │   ├── lecture.py     # 讲义生成
│   │   │   ├── qa.py          # 苏格拉底式答疑
│   │   │   ├── diagnosis.py   # 诊断性问题
│   │   │   ├── review.py      # 复习内容生成
│   │   │   ├── summary.py     # 个性化摘要
│   │   │   ├── note_organize.py # 笔记整理 Prompt（Sprint 9）
│   │   │   └── note_organize_agent.py # 整理到笔记 Agent Prompt（Sprint 10）
│   │   ├── rag/               # RAG 管道
│   │   │   ├── chunking.py    # 文档分块
│   │   │   └── embedding.py   # 向量嵌入
│   │   ├── web_search/        # 网络搜索
│   │   │   ├── searcher.py
│   │   │   └── scraper.py
│   │   ├── llm_client.py      # LLM 客户端（支持流式）
│   │   └── llm_router.py      # 模型路由
│   ├── api/
│   │   ├── deps.py            # 依赖注入
│   │   └── v1/                # API v1 路由
│   │       ├── auth.py        # 认证
│   │       ├── users.py       # 用户
│   │       ├── notes.py       # 笔记
│   │       ├── tags.py        # 标签
│   │       ├── learning.py    # 学习引擎
│   │       ├── knowledge.py   # 知识图谱
│   │       ├── qa.py          # 智能答疑
│   │       ├── review.py      # 复习系统
│   │       ├── search.py      # 全局搜索（Phase 3）
│   │   │   ├── agent.py       # Agent 会话（Phase 3）
│   │   │   ├── websocket.py   # WebSocket 端点
│   │   │   └── router.py      # 路由注册
│   ├── core/                  # 核心模块
│   │   ├── cancel_token.py    # Redis 取消令牌（Phase 3）
│   │   ├── exceptions.py      # 自定义异常
│   │   ├── middleware.py      # 中间件
│   │   ├── security.py        # 安全工具
│   │   └── utils.py           # 工具函数
│   ├── models/                # ORM 模型
│   │   ├── user.py            # 用户
│   │   ├── note.py            # 笔记
│   │   ├── tag.py             # 标签
│   │   ├── knowledge.py       # 知识图谱
│   │   ├── learning.py        # 学习引擎
│   │   ├── review.py          # 复习
│   │   ├── memory.py          # 记忆系统
│   │   ├── rag.py             # RAG
│   │   ├── agent_session.py   # Agent 会话（Phase 3）
│   │   ├── agent_message.py   # Agent 消息（Phase 3）
│   │   └── auth.py            # 认证
│   ├── schemas/               # Pydantic Schema
│   │   ├── common.py          # 通用 Schema
│   │   ├── user.py
│   │   ├── note.py
│   │   ├── knowledge.py
│   │   ├── learning.py
│   │   ├── qa.py
│   │   ├── review.py
│   │   └── agent.py           # Agent Schema（Phase 3）
│   ├── services/              # 业务逻辑层
│   │   ├── auth_service.py
│   │   ├── note_service.py
│   │   ├── knowledge_service.py
│   │   ├── learning_service.py
│   │   ├── qa_service.py
│   │   ├── review_service.py
│   │   ├── graph_db.py        # Apache AGE 封装
│   │   ├── search_service.py  # Meilisearch（4 索引 + 全量同步）
│   │   ├── user_memory_service.py
│   │   ├── agent/             # Agent 服务（Phase 3）
│   │   │   ├── agent_service.py    # 会话/消息 CRUD
│   │   │   ├── agent_loop.py       # ReAct 推理循环
│   │   │   ├── context_builder.py  # 上下文注入
│   │   │   ├── tool_registry.py    # 工具注册表
│   │   │   ├── tool_schemas.py     # 工具 Schema
│   │   │   └── handlers/           # 工具处理器（7 模块 14 工具）
│   │   └── memory/            # 记忆系统
│   │       ├── models.py
│   │       ├── repository.py
│   │       └── user_memory.py
│   ├── tasks/                 # Celery 异步任务
│   │   ├── celery_app.py      # Celery 配置
│   │   ├── learning_tasks.py  # 学习路线/讲义生成（Sprint 8 升级）
│   │   ├── note_tasks.py      # AI 整理笔记任务（Sprint 9）
│   │   ├── lecture_tasks.py   # 旧版讲义生成（未注册，deprecated）
│   │   └── review_tasks.py    # 复习提醒
│   ├── config.py              # 配置管理
│   ├── database.py            # 数据库连接
│   └── main.py                # FastAPI 入口
├── nginx/                     # Nginx 反向代理（Sprint 9）
│   ├── nginx.conf             # Nginx 配置
│   └── entrypoint.sh          # 自签名证书 + 启动脚本
├── scripts/                   # 运维脚本
│   ├── generate-secrets.sh    # 密钥生成
│   └── init-db.sql            # 数据库初始化
├── secrets/                   # Docker Secrets 密钥文件
├── tests/                     # 测试
├── docker-compose.yml         # Docker 编排
├── pyproject.toml             # 项目依赖
└── .venv/                     # 虚拟环境
```

### 2.2 核心架构特性

1. **全异步架构**：FastAPI + SQLAlchemy 2.0 async + asyncpg
2. **依赖注入**：FastAPI Depends 机制管理数据库会话和用户认证
3. **分层设计**：API 路由 → Service 服务 → ORM 模型
4. **异步任务**：Celery + Redis 处理 LLM 调用、路线生成等耗时操作
5. **WebSocket 实时通信**：支持讲义进度、答疑流式输出、复习提醒、Agent 对话
6. **图数据库集成**：Apache AGE 扩展支持知识图谱查询
7. **RAG 支持**：向量嵌入 + 文档分块 + 检索增强生成
8. **Agent 智能体**：ReAct 模式 AI 助手，14 工具调用，多轮对话，上下文感知，笔记编辑（行级+列级）
9. **笔记 AI 交互**：划词问 AI、整理到笔记（隐藏 Agent 会话驱动）、进度实时推送

---

## 三、开发阶段与进度

### Phase 1：基础搭建（P0）✅ 已完成

**时间范围**：Sprint 1 - Sprint 4  
**完成日期**：2026-07-08  
**目标**：搭建项目基础设施，跑通核心链路

#### Sprint 1：项目初始化 ✅

**交付内容**：
- ✅ 完整项目目录结构
- ✅ Docker Compose 配置（PostgreSQL + Redis + Meilisearch）
- ✅ FastAPI 应用骨架
- ✅ 全部 ORM 模型定义（11 个模型）
- ✅ Alembic 数据库迁移
- ✅ 代码规范配置（Ruff + MyPy）

**核心模型**：
- `User` / `UserProfile` / `PersonalHabitProfile` - 用户体系
- `Note` / `Tag` / `NoteTag` - 笔记系统
- `KnowledgeNode` / `KnowledgeEdge` / `UserKnowledgeMastery` - 知识图谱
- `LearningRoute` / `LearningRouteStep` / `Lecture` / `LearningRecord` - 学习引擎
- `QASession` / `QAMessage` - 答疑系统
- `ReviewPlan` - 复习系统
- `MemoryIndex` / `MemoryFull` - 记忆系统
- `RefreshToken` - 认证令牌

#### Sprint 2：认证与笔记基础 ✅

**交付内容**：
- ✅ JWT 认证全流程（注册/登录/刷新/登出）
- ✅ 笔记 CRUD 接口
- ✅ Meilisearch 全文搜索集成
- ✅ 标签系统（系统标签 + 自定义标签）

**API 端点**：
```
POST   /api/v1/auth/register      # 用户注册
POST   /api/v1/auth/login         # 登录
POST   /api/v1/auth/refresh       # 刷新令牌
POST   /api/v1/auth/logout        # 登出
GET    /api/v1/notes              # 笔记列表（分页+筛选）
POST   /api/v1/notes              # 创建笔记
GET    /api/v1/notes/{id}         # 笔记详情
PUT    /api/v1/notes/{id}         # 更新笔记
DELETE /api/v1/notes/{id}         # 删除笔记
GET    /api/v1/tags               # 标签列表
POST   /api/v1/tags               # 创建标签
POST   /api/v1/tags/notes/{id}    # 添加笔记标签
DELETE /api/v1/tags/notes/{id}/{tid} # 移除笔记标签
```

**核心实现**：
- `AuthService` - 认证服务（密码哈希、JWT 生成/验证、Refresh Token 管理）
- `NoteService` - 笔记服务（CRUD + 全文搜索）
- `search_service.py` - Meilisearch 集成（索引初始化、笔记索引/搜索）

#### Sprint 3：AI 核心链路 ✅

**交付内容**：
- ✅ LLM 适配层（DeepSeek API，OpenAI 兼容接口）
- ✅ RAG 基础版（文档分块 + 向量嵌入）
- ✅ 讲义生成异步任务（Celery）
- ✅ 网络搜索集成（搜索 + 抓取）
- ✅ WebSocket 流式输出

**核心实现**：
- `LLMClient` - 统一 LLM 客户端（支持同步/流式调用）
- `LLMResponse` - 统一响应封装
- `get_llm_client()` - 全局单例
- RAG 管道：`chunking.py` + `embedding.py`
- 网络搜索：`searcher.py` + `scraper.py`

**Celery 任务**：
- `generate_lecture_task` - 异步生成讲义

#### Sprint 4：用户记忆与画像 ✅

**交付内容**：
- ✅ 双层记忆存储（索引层 + 完整层）
- ✅ 个人习惯画像（Push 模式）
- ✅ 知识画像（Pull 模式）
- ✅ UserMemory 封装
- ✅ 个性化知识梳理

**核心实现**：
- `memory/models.py` - 记忆数据模型（MemoryEntry, HabitSummary）
- `memory/repository.py` - 记忆存储层
- `memory/user_memory.py` - UserMemory 封装
- `user_memory_service.py` - 用户记忆服务（掌握度更新、行为统计、画像摘要）

**API 端点**：
```
GET  /api/v1/users/me          # 获取当前用户信息
PATCH /api/v1/users/me         # 更新用户信息
GET  /api/v1/users/me/profile  # 获取知识画像摘要
```

---

### Phase 2：核心功能完善（P1）✅ 已完成

**时间范围**：Sprint 5 - Sprint 8  
**完成日期**：2026-07-10  
**目标**：实现三大核心学习功能 + 讲义增强与笔记联动

#### Sprint 5：学习路线与知识图谱 ✅

**交付内容**：
- ✅ Apache AGE 图数据库封装层（GraphDB）
- ✅ 知识图谱服务（节点/边 CRUD + 图查询 + 路径查询）
- ✅ LLM 驱动学习路线生成（知识图谱 + Prompt 工程）
- ✅ 路线动态调整（表现不佳加练习，表现优秀合并步骤）
- ✅ 知识图谱 API

**核心实现**：

**1. Apache AGE 封装（`graph_db.py`）**：
- `GraphDB` 静态方法类
- Cypher 查询执行（读/写分离）
- 知识节点/边创建（自动同步到 AGE 图）
- 图查询：前置依赖、后续依赖、关联节点、子知识点、最短路径
- 按学科查询节点及前置依赖

**2. 知识图谱服务（`knowledge_service.py`）**：
- 节点 CRUD（创建后同步到 AGE 图，失败不阻塞）
- 边 CRUD
- 图查询（AGE 失败降级到关系表）
- 路径查询（最短路径）
- 学科列表（含中文 display_name 映射）
- 掌握度管理（指数移动平均算法）
- 初始数据导入

**3. 学习引擎升级（`learning_service.py`）**：
- `create_route` - 创建路线（异步触发 Celery 任务）
- `generate_route_with_llm` - LLM + 知识图谱生成路线：
  - 获取用户已掌握知识点
  - 推断学科（关键词匹配）
  - 从知识图谱获取节点及前置依赖
  - 构建 Prompt → 调用 LLM → 解析 JSON → 创建步骤
- `complete_step` - 标记步骤完成 + 动态调整：
  - 记录学习活动
  - 更新知识掌握度
  - 表现不佳（用时/预估 > 2）→ 添加补充练习
  - 表现优秀（用时/预估 < 0.5）→ 合并后续步骤
- `create_lecture` - 创建讲义（传入 route_id + step_id）并异步生成
- `generate_lecture_content` - **Sprint 8 升级版讲义生成流程**：
  - 获取路线主题 + 步骤上下文（title/description）
  - 获取知识节点详情（name/description/difficulty）
  - 获取前置依赖节点
  - 获取用户掌握度（自适应难度）
  - **RAG 检索**（知识库向量搜索 top 5）
  - **网络搜索**（补充最新参考材料）
  - 合并用户自定义指令
  - 构建增强 Prompt → 调用 LLM 生成讲义
  - 自动创建关联笔记（Note）并打标签
  - 通过 **Redis Pub/Sub** 实时推送进度到前端
- 路线删除（单条/批量）

**4. Prompt 模板（`prompts/route.py`）**：
- `ROUTE_GENERATION_PROMPT` - 学习路线生成
- 输入：学生水平、已掌握知识点、目标、可用时间、学习主题、可用知识点（含前置依赖）
- 输出：JSON 格式（title, description, estimated_total_hours, steps 数组）

**5. 讲义生成 Prompt（`prompts/lecture.py`，Sprint 8 升级）**：
- `LECTURE_GENERATION_PROMPT` - 讲义生成（增强版）
- 输入变量：
  - `topic` - 学科/主题
  - `step_title` / `step_description` - 当前步骤上下文
  - `node_name` / `node_description` - 知识点详情
  - `difficulty` / `student_level` - 难度与水平
  - `prerequisites` - 前置知识点
  - `retrieved_context` - RAG + 网络搜索 + 用户指令合并的参考材料
- 输出要求：核心概念 → 关键公式/定理 → 典型例题 → 常见误区 → 知识关联 → 自我检测

**6. Celery 任务（`learning_tasks.py`）**：
- `generate_learning_route` - 异步生成学习路线
- `generate_lecture_content_task` - 异步生成讲义（Sprint 8 升级，签名：lecture_id, user_id, node_id, route_id, step_id, custom_instructions）

**7. 旧版讲义任务（`lecture_tasks.py`，deprecated）**：
- `generate_lecture_task` - 旧版讲义生成（未注册到 Celery include，已被 learning_tasks 替代）

**API 端点**：
```
GET    /api/v1/knowledge/subjects              # 获取学科列表
GET    /api/v1/knowledge/nodes                 # 查询知识节点（分页+筛选）
POST   /api/v1/knowledge/nodes                 # 创建知识节点
GET    /api/v1/knowledge/nodes/{id}/graph      # 获取节点图结构
GET    /api/v1/knowledge/nodes/{id}/path       # 获取学习路径
POST   /api/v1/knowledge/edges                 # 创建知识边
POST   /api/v1/learning/routes                 # 创建学习路线
GET    /api/v1/learning/routes                 # 路线列表
GET    /api/v1/learning/routes/{id}            # 路线详情
PATCH  /api/v1/learning/routes/{id}/steps/{sid}/complete  # 完成步骤
DELETE /api/v1/learning/routes/{id}            # 删除路线
DELETE /api/v1/learning/routes/batch           # 批量删除路线
POST   /api/v1/learning/lectures/generate      # 生成讲义
GET    /api/v1/learning/lectures/{id}          # 获取讲义
```

**WebSocket 端点**：
```
WS /ws/lecture-progress/{lecture_id}   # 讲义生成进度
WS /ws/route-progress/{route_id}       # 学习路线生成进度
```

#### Sprint 6：智能答疑 ✅

**交付内容**：
- ✅ 苏格拉底式答疑服务（引导式教学，不直接给答案）
- ✅ 诊断性问题生成（多认知层次）
- ✅ QA 流式 WebSocket（逐 token 推送）
- ✅ 答疑中实时更新知识画像

**核心实现**：

**1. 答疑服务（`qa_service.py`）**：
- 会话管理：创建/获取/列表/关闭
- 消息处理（苏格拉底式）：
  - 保存用户消息
  - 构建上下文（知识节点名称、掌握度、讲义摘要）
  - 构建消息列表（system + 最近 10 轮历史）
  - 调用 LLM
  - 保存 AI 回复
  - 分析用户回答质量更新掌握度（LLM 评分 0-1）
- 流式消息：通过 WebSocket 逐 token 推送
  - 推送类型：token（逐 token）、done（完成）、diagnosis（掌握度变化）
- 诊断性问题生成：基于讲义 + 掌握度
- 消息历史（支持 before 游标分页）

**2. Prompt 模板**：
- `QA_SYSTEM_PROMPT` - 苏格拉底式答疑系统 Prompt
  - 行为准则：不直接给答案、使用类比、肯定正确、引导错误
  - 变量：node_name, lecture_summary, mastery_score
- `DIAGNOSIS_PROMPT` - 诊断性问题生成
  - 生成 3-5 个问题，覆盖不同认知层次（记忆、理解、应用、分析）
  - 输出 JSON 格式

**3. LLM 客户端升级**：
- 新增 `chat_stream` 异步生成器方法
- 逐 chunk yield content，支持 WebSocket 流式推送

**API 端点**：
```
POST /api/v1/learning/qa/sessions                    # 创建答疑会话
GET  /api/v1/learning/qa/sessions                    # 会话列表
POST /api/v1/learning/qa/sessions/{id}/messages      # 发送消息（非流式）
GET  /api/v1/learning/qa/sessions/{id}/messages      # 历史消息
POST /api/v1/learning/qa/diagnostic-questions        # 生成诊断性问题
```

**WebSocket 端点**：
```
WS /ws/qa-stream/{session_id}   # 答疑对话流式输出
```

#### Sprint 7：复习系统 ✅

**交付内容**：
- ✅ SM-2 间隔重复算法
- ✅ 复习计划管理（创建/列表/完成）
- ✅ Celery Beat 定时提醒（每 5 分钟检查到期复习）
- ✅ 复习内容自动生成（根据掌握度选择闪卡/选择题/讲解）

**核心实现**：

**1. 复习服务（`review_service.py`）**：
- SM-2 算法：
  - `calculate_next_review` - 核心算法
    - quality >= 3: interval 递增（0→1→6→round(interval*ef)），ef 更新
    - quality < 3: interval 重置为 1，ef 降低 0.2（最低 1.3）
  - `quality_from_performance` - 0-1 分数转 0-5 质量评分
- 复习计划管理：
  - `schedule_review` - 创建/更新复习计划
  - `get_review_plans` - 列表（含节点名称，支持 status/日期范围筛选+分页）
  - `complete_review` - 完成复习：标记 completed → SM-2 计算 → 更新掌握度 → 自动创建下次计划
- 复习统计：今日到期、本周完成、逾期数、掌握度分布
- 复习内容生成：
  - 根据掌握度自动选择类型：>0.7→flashcard, 0.4-0.7→quiz, <0.4→explanation
  - 调用 LLM 生成
- 到期检查：`check_due_reviews` - Celery Beat 调用

**2. Prompt 模板（`prompts/review.py`）**：
- `REVIEW_GENERATION_PROMPT` - 复习内容生成
- 变量：node_name, mastery_score, review_count, last_performance, review_strategy
- 根据复习类型输出：flashcard(3-5 张闪卡), quiz(3-5 道题), explanation(重新讲解+练习)

**3. Celery Beat 配置**：
```python
celery_app.conf.beat_schedule = {
    "check-review-reminders": {
        "task": "app.tasks.review_tasks.check_review_reminders",
        "schedule": 300.0,  # 每 5 分钟
    },
}
```

**4. WebSocket 升级**：
- `/ws/notifications` - 使用 `user_{user_id}` 作为 client_id，支持按用户推送复习提醒

**API 端点**：
```
GET  /api/v1/review/plans              # 复习计划列表（筛选+分页）
POST /api/v1/review/plans/{id}/complete # 完成复习（SM-2 重算）
POST /api/v1/review/generate-content   # 生成复习内容
GET  /api/v1/review/stats              # 复习统计
```

**WebSocket 端点**：
```
WS /ws/notifications   # 系统通知（复习提醒等）
```

#### Sprint 8：讲义增强与笔记联动 ✅

**时间范围**：2026-07-09 ~ 2026-07-10  
**目标**：讲义生成自动创建笔记、增强 Prompt 上下文、修复跨进程通信问题

**交付内容**：
- ✅ 讲义生成自动创建笔记（Lecture.note_id 关联 Note）
- ✅ 自动标签（知识节点名称 + 学科中文名）
- ✅ 讲义 Prompt 增强（新增 step_title/step_description/node_description/prerequisites 变量）
- ✅ RAG 检索 + 网络搜索集成到讲义生成流程
- ✅ 讲义生成进度推送完善（新增 preparing_context 阶段）
- ✅ Redis Pub/Sub 跨进程通信（Celery worker → FastAPI WebSocket）
- ✅ 修复 Celery event loop 冲突（同步 Redis 替代 async Redis 发布）
- ✅ Celery 任务签名升级（新增 route_id/step_id 参数）

**数据库变更**：
- `lectures` 表新增 `note_id` 字段（外键 → notes.id，ON DELETE SET NULL）
- Alembic 迁移：`c0826ebcce52_add_note_id_to_lectures.py`

**讲义生成新流程**（`generate_lecture_content`）：
1. 获取路线主题（route_topic）
2. 获取步骤上下文（step_title / step_description）
3. 获取知识节点详情（name / description / difficulty）
4. 获取前置依赖节点（prerequisites）
5. 获取用户掌握度（自适应难度等级）
6. RAG 检索（知识库向量搜索 top 5）
7. 网络搜索（补充最新参考材料）
8. 合并用户自定义指令
9. 构建增强 Prompt → 调用 LLM 生成
10. 保存讲义 + 自动创建关联笔记（Note）并打标签
11. 通过 Redis Pub/Sub 推送 complete 消息（含 note_id）

**Prompt 模板升级**（`prompts/lecture.py`）：
- 新增 `{step_title}` / `{step_description}` - 当前步骤上下文
- 新增 `{node_description}` - 知识点详细描述
- 新增 `{prerequisites}` - 前置知识列表
- 强化主题约束："严格围绕主题下的知识点展开，不要偏离到通用学习技能"

**WebSocket 进度阶段**：
| 阶段 | 百分比 | 说明 |
|------|--------|------|
| connecting | 0% | WebSocket 连接建立 |
| preparing_context | 10% | 任务启动，查询知识图谱/RAG/网络搜索 |
| generating_content | 30% | 调用 LLM 生成讲义 |
| finalizing | 80% | LLM 返回，保存 + 创建笔记 |
| complete | - | 完成，携带 lecture 数据 + note_id |
| error | - | 异常推送错误消息 |

**Redis Pub/Sub 跨进程通信**：
- 发布端（Celery worker）：使用同步 Redis 客户端（`redis.Redis`），避免 event loop 冲突
- 订阅端（FastAPI WebSocket）：每次连接创建新的 async Redis 客户端
- 修复：`redis.asyncio` 在 Celery 中因 event loop 关闭导致 `RuntimeError: Event loop is closed`

---

### Phase 3：Agent 智能体与搜索增强（P2）✅ 已完成

**时间范围**：2026-07-10 ~ 2026-07-11  
**完成日期**：2026-07-11  
**目标**：实现 AI 学习助手（多轮对话 + 工具调用）、全局搜索增强、数据级联修复

#### 3.1 Agent 智能体 ✅

**交付内容**：
- ✅ Agent 会话管理（创建/列表/详情/关闭/删除/批量删除）
- ✅ 消息收发（HTTP 非流式 + WebSocket 流式）
- ✅ ReAct 推理循环（`AgentLoop`，最大 6 轮推理，4000 token 预算）
- ✅ 工具注册系统（`ToolRegistry` + `ToolSchema`，12 个工具跨 7 个模块，Sprint 9 增至 14）
- ✅ 上下文注入（`ContextBuilder`，根据 context_type 自动注入笔记/讲义/路线摘要）
- ✅ 用户记忆集成（Push 模式习惯摘要注入 system prompt）
- ✅ 取消生成（Redis-based cancel token，支持中途停止）
- ✅ 工具调用 WebSocket 实时推送（thinking / tool_call_start / tool_call_result / token / done / error）

**数据库变更**：
- 新增 `agent_sessions` 表（会话，含 context_type/context_id 上下文关联）
- 新增 `agent_messages` 表（消息，含 tool_calls JSONB、metadata JSONB）
- Alembic 迁移：`20260710_add_agent_tables.py`

**核心实现**：

**1. Agent 服务层（`agent_service.py`）**：
- 会话 CRUD（create/list/get/close/delete/batch_delete）
- 消息管理（save_user_message/get_messages/build_conversation_history）
- 取消生成（cancel_generation，通过 Redis cancel token）

**2. Agent 循环（`agent_loop.py`）**：
- ReAct 模式：思考 → 决定工具调用 → 执行 → 观察 → 重复
- 安全控制：max_steps（最大推理轮数）、token_budget（最大 token 消耗）
- 写操作限流：category="write" 的工具每轮最多调用 1 次
- 流式 token 推送（WebSocket）+ 非流式返回（HTTP）
- 工具调用拆分为 start + result 两条 WS 消息

**3. 工具系统（`tool_registry.py` + `tool_schemas.py`）**：
- `ToolSchema`：name / display_name / description / parameters / category（read|write）/ module / icon
- `ToolResult`：success / data / error_message / token_count
- `ToolRegistry`：register / execute / get_all_schemas
- `init_tool_registry()`：应用启动时注册所有工具

**4. 已注册工具清单（12 个，Sprint 9 增至 14 个）**：

| 工具名 | 模块 | 描述 | 类别 |
|--------|------|------|------|
| search_notes | note | 搜索笔记 | read |
| get_note_content | note | 获取笔记内容 | read |
| edit_note | note | 行级笔记编辑（Sprint 9） | write |
| search_knowledge | learning | 搜索知识图谱 | read |
| get_mastery | learning | 获取知识掌握度 | read |
| get_route_progress | learning | 获取路线进度 | read |
| get_qa_history | qa | 获取答疑历史 | read |
| socratic_qa | qa | 苏格拉底式答疑（Sprint 9） | read |
| get_review_status | review | 获取复习状态 | read |
| schedule_review | review | 安排复习计划 | write |
| global_search | search | 全局搜索（笔记/讲义/知识点/路线） | read |
| semantic_search | search | 语义相似度搜索 | read |
| recall_knowledge | memory | 回忆知识点 | read |
| search_memory | memory | 搜索记忆 | read |

**5. 上下文构建器（`context_builder.py`）**：
- `note` 上下文：注入笔记标题、学科、标签、内容摘要（前 500 字）
- `lecture` 上下文：注入讲义标题、关联知识点、内容摘要
- `route` 上下文：注入路线主题、步骤列表、完成进度

**6. Agent Prompt（`prompts/agent.py`）**：
- `AGENT_SYSTEM_PROMPT` - Agent 系统 Prompt
- 变量：habit_summary（用户习惯）、context_text（当前上下文）、tool_descriptions（可用工具）、current_time
- 行为准则：先理解需求再调用工具、简要告知用户、组织总结工具输出、不编造数据

**7. 取消令牌（`cancel_token.py`）**：
- Redis-based 异步取消令牌
- set/clear/is_cancelled 三个操作
- TTL 300 秒自动过期

**API 端点**：
```
POST   /api/v1/agent/sessions              # 创建 Agent 会话
GET    /api/v1/agent/sessions              # 会话列表
GET    /api/v1/agent/sessions/{id}         # 会话详情
POST   /api/v1/agent/sessions/{id}/close   # 关闭会话
DELETE /api/v1/agent/sessions/{id}         # 删除会话
POST   /api/v1/agent/sessions/batch-delete # 批量删除会话
POST   /api/v1/agent/sessions/{id}/messages # 发送消息（HTTP 非流式）
GET    /api/v1/agent/sessions/{id}/messages # 获取历史消息
POST   /api/v1/agent/sessions/{id}/cancel  # 取消生成
GET    /api/v1/agent/tools                  # 获取可用工具列表
```

**WebSocket 端点**：
```
WS /api/v1/ws/agent/{session_id}?token={access_token}  # Agent 对话
```

**WebSocket 消息类型**：
| 方向 | 类型 | 说明 |
|------|------|------|
| 客户端→服务端 | message | 用户消息 |
| 客户端→服务端 | cancel | 取消生成 |
| 客户端→服务端 | pong | 心跳响应 |
| 服务端→客户端 | session_created | 会话建立确认 |
| 服务端→客户端 | thinking | 思考中 |
| 服务端→客户端 | tool_call_start | 工具调用开始 |
| 服务端→客户端 | tool_call_result | 工具调用结果 |
| 服务端→客户端 | token | 流式 token |
| 服务端→客户端 | done | 生成完成 |
| 服务端→客户端 | error | 错误消息 |

#### 3.2 全局搜索增强 ✅

**交付内容**：
- ✅ Meilisearch 4 索引初始化（notes / lectures / knowledge_nodes / learning_routes）
- ✅ 索引 primaryKey 自动修复（`_ensure_index()` 检测 null primaryKey 并重建）
- ✅ 启动时全量数据同步（`sync_all_to_meilisearch()`）
- ✅ 全局搜索 API 重写（Meilisearch + DB 模糊搜索双通道）
- ✅ 语义搜索 API
- ✅ 手动重建索引 API（`POST /search/reindex`）
- ✅ 笔记 CRUD 自动同步到 Meilisearch

**核心实现**：

**1. 搜索服务升级（`search_service.py`）**：
- `_ensure_index()` — 检测索引 primaryKey 是否为 null，若是则删除重建
- `init_meilisearch_indexes()` — 初始化 4 个索引并配置 searchable/filterable/sortable 属性
- 全量同步方法：`sync_all_notes_to_meilisearch()` / `sync_all_knowledge_nodes()` / `sync_all_lectures()` / `sync_all_learning_routes()`
- `sync_all_to_meilisearch()` — 一键全量同步
- 单条操作：`index_note()` / `delete_note_index()` / `search_notes_meilisearch()`

**2. 搜索 API 重写（`search.py`）**：
- `GET /search/global` — 全局搜索，支持 type 参数（all/notes/lectures/knowledge/routes）
  - 先查 Meilisearch 各索引，无结果时退回数据库 ILIKE 模糊搜索
  - 支持高亮、分面统计、分页
- `GET /search/semantic` — 语义搜索（Meilisearch + DB 兜底）
- `POST /search/reindex` — 手动触发全量重建索引

**3. 笔记服务同步（`note_service.py`）**：
- `_sync_note_to_meilisearch()` — 创建/更新笔记时同步索引
- `_delete_note_from_meilisearch()` — 删除笔记时清理索引
- 在 create_note / update_note / delete_note / batch_delete_notes 中调用

**4. 应用启动同步（`main.py`）**：
- lifespan 启动时依次调用 `init_meilisearch_indexes()` + `sync_all_to_meilisearch()`

**Meilisearch 索引配置**：
| 索引 | searchable | filterable | sortable |
|------|-----------|-----------|----------|
| notes | title, content, subject | user_id, subject, created_at, updated_at | created_at, updated_at, word_count |
| lectures | title, content | user_id, status, created_at | - |
| knowledge_nodes | name, description, subject | subject, grade_level, difficulty | - |
| learning_routes | topic, description | user_id, status | - |

#### 3.3 笔记删除级联修复 ✅

**问题**：删除笔记后，关联讲义（`Lecture.note_id`）被 SET NULL 成为孤立数据，搜索仍显示大量已删除笔记的讲义。

**修复内容**：
- ✅ `note_service.py` 新增 `_delete_associated_lectures()` — 删除笔记时级联删除关联讲义并清理 Meilisearch 索引
- ✅ `delete_note` 和 `batch_delete_notes` 均已接入级联删除
- ✅ `learning_service.py` 的 `delete_route` 和 `batch_delete_routes` 升级 — 删除路线时级联清理关联讲义及 Meilisearch 索引
- ✅ 新增 `_delete_route_lectures()` 辅助方法
- ✅ 清理已存在的 46 条孤立讲义并同步 Meilisearch

**Agent 配置参数**（`config.py`）：
```python
AGENT_MAX_STEPS = 6              # 最大推理轮数
AGENT_TOKEN_BUDGET = 16000       # 单次对话最大 token
AGENT_MAX_CONVERSATION_TURNS = 10 # 最大对话轮数
AGENT_AUTO_TITLE = True          # 自动生成会话标题
AGENT_CANCEL_TTL = 300           # 取消令牌过期时间（秒）
```

**数据库变更**：
- `agent_sessions` 表 + `agent_messages` 表（见 3.1）
- 嵌入维度调整：`memory_index.embedding` 和 `document_chunks.embedding` 从 vector(1024) 改为 vector(512)
- Alembic 迁移：`20260711_change_embedding_dim_to_512.py`

---

### Phase 4：Sprint 9 — AI 整理笔记 + Agent 工具增强 + 生产部署加固 ✅ 已完成

**时间范围**：2026-07-11 ~ 2026-07-14  
**完成日期**：2026-07-14  
**目标**：实现 AI 笔记整理功能、Agent 工具增强（14 工具）、配置安全加固、Docker 生产环境部署

#### 4.1 AI 整理笔记 ✅

**交付内容**：
- ✅ 笔记整理 API（`POST /notes/organize`，支持 1-20 篇笔记）
- ✅ Celery 异步整理任务（`organize_notes_task`，LLM 驱动）
- ✅ 整理进度 WebSocket（`/ws/organize-progress/{task_id}`，Redis Pub/Sub）
- ✅ 整理溯源字段（`origin_type` + `source_note_ids`，记录 AI 整理来源）
- ✅ 整理 Prompt 模板（合并/摘要/对比/扩展/纠错等多种方式）
- ✅ 成果笔记自动同步 Meilisearch 索引

**数据库变更**：
- `notes` 表新增 `origin_type` 字段（String(20)，默认 "user"，可选 "ai_organized"）
- `notes` 表新增 `source_note_ids` 字段（ARRAY(UUID)，记录源笔记 ID 列表）
- Alembic 迁移：`20260711_add_note_organize_fields.py`

**核心实现**：

**1. 笔记服务升级（`note_service.py`）**：
- `organize_notes()` — 验证笔记归属、构建笔记数据、提交 Celery 任务
- `create_organized_note()` — 创建 AI 整理成果笔记（设置 origin_type + source_note_ids）
- 新增 `edit_note` 工具 Schema（行级编辑：insert/replace/delete）

**2. Celery 任务（`note_tasks.py`）**：
- `organize_notes_task` — 异步整理任务：
  1. 解析笔记数据，构建多笔记内容文本
  2. 调用 LLM（system + user prompt）生成整理结果
  3. 解析标题（`# ` 开头）和正文
  4. 创建成果笔记（origin_type="ai_organized"，source_note_ids 记录来源）
  5. 同步 Meilisearch 索引
  6. 通过 Redis Pub/Sub 推送进度（preparing → generating → complete/error）

**3. Prompt 模板（`prompts/note_organize.py`）**：
- `NOTE_ORGANIZE_SYSTEM_PROMPT` — 整理专家系统 Prompt
  - 支持方式：合并重组、摘要提炼、对比分析、知识扩展、纠错优化、自定义
  - 输出要求：Markdown 格式，首行 `# 标题`，然后正文
- `NOTE_ORGANIZE_USER_PROMPT` — 用户整理请求模板
  - 变量：`note_count`、`user_prompt`、`notes_content`

**4. WebSocket 进度端点**：
- 路径：`/ws/organize-progress/{task_id}`（app 级别路由，不带 `/api/v1` 前缀）
- 认证：JWT token 通过 Query 参数传递
- Redis Pub/Sub 频道：`organize_progress:{task_id}`

**进度阶段**：
| 阶段 | 百分比 | 说明 |
|------|--------|------|
| preparing | 10% | 任务启动，准备笔记内容 |
| generating | 30% | AI 正在整理笔记 |
| complete | 100% | 整理完成，携带 note_id/title/word_count/source_count |
| error | - | 整理失败，推送错误信息 |

**API 端点**：
```
POST /api/v1/notes/organize      # AI 整理笔记（异步，返回 task_id）
```

**WebSocket 端点**：
```
WS /ws/organize-progress/{task_id}?token={access_token}   # 整理进度监听
```

**Schema**：
```python
class OrganizeNotesRequest(BaseModel):
    note_ids: list[uuid.UUID]   # 1-20 篇笔记 ID
    prompt: str                  # 额外提示词（整理方式/要求）

class OrganizeNotesResponse(BaseModel):
    task_id: str                 # Celery 任务 ID
    message: str                 # 状态消息
```

#### 4.2 Agent 工具增强（12 → 14 工具） ✅

**交付内容**：
- ✅ 新增 `edit_note` 工具（行级笔记编辑：insert/replace/delete）
- ✅ 新增 `socratic_qa` 工具（Agent 内苏格拉底式答疑）
- ✅ 搜索工具全面升级（优先 Meilisearch 索引，回退 PostgreSQL）
- ✅ Token 计数优化（工具返回结果自动估算 token 数）

**新增工具清单**：

| 工具名 | 模块 | 描述 | 类别 |
|--------|------|------|------|
| edit_note | note | 行级笔记编辑（insert/replace/delete） | write |
| socratic_qa | qa | 苏格拉底式答疑（引导式教学） | read |

**edit_note 工具详情**：
- 操作类型：`insert`（插入）、`replace`（替换）、`delete`（删除）
- 行号规则：1-based（第一行 = 1）
- insert：在 `start_line` 行前插入内容，`end_line` 忽略
- replace：替换 `start_line` 到 `end_line`（含）的内容
- delete：删除 `start_line` 到 `end_line`（含）的内容
- 编辑后自动同步 Meilisearch + WebSocket 推送 `note_edit` 事件到前端

**socratic_qa 工具详情**：
- Agent 判定用户问题为概念理解类时自动调用
- 使用苏格拉底导师模式生成引导式回答（不直接给答案）
- Prompt 模板：`SOCRATIC_QA_TOOL_PROMPT`（`prompts/qa.py`）
- 支持上下文注入（topic + context）

**搜索工具升级**：
- `search_notes`：优先 Meilisearch 索引搜索，无结果回退 PostgreSQL ILIKE
- `global_search`：笔记/讲义/知识节点/路线均优先 Meilisearch
- `search_knowledge`：知识节点搜索优先 Meilisearch

**Token 计数优化**：
- `ToolRegistry.execute()` 现在对字符串/dict 返回值自动估算 token 数（`len // 2`）
- Agent 循环的 token_budget 统计更精确

#### 4.3 配置安全加固 ✅

**交付内容**：
- ✅ `_read_secret()` 统一密钥读取函数（Docker secrets → 本地 secrets/ → 环境变量 → 默认值）
- ✅ 所有敏感配置改为 `@property` 动态读取（DATABASE_URL、REDIS_URL、JWT_SECRET_KEY 等）
- ✅ 数据库/Redis URL 拆分为独立字段（HOST/PORT/USER/NAME），动态拼接
- ✅ Agent token 预算从 4000 提升至 16000
- ✅ 新增 Sprint 9 AI 整理笔记配置项

**配置读取优先级**：
1. Docker secrets 文件（`/run/secrets/` 或 `SECRETS_DIR`）
2. 项目本地 `secrets/` 目录（开发环境）
3. 系统环境变量
4. 默认值

**新增配置项**：
```python
ORGANIZE_MAX_NOTES: int = 20           # 单次最多整理笔记数
ORGANIZE_MAX_PROMPT_LENGTH: int = 2000 # 提示词最大长度
ORGANIZE_LLM_TEMPERATURE: float = 0.7  # 整理 LLM 温度
ORGANIZE_LLM_MAX_TOKENS: int = 4096    # 整理 LLM 最大 token
```

#### 4.4 生产环境部署加固 ✅

**交付内容**：
- ✅ Docker 多阶段构建（builder + production，减小镜像体积）
- ✅ 非 root 用户运行（`qingyun` 用户，安全加固）
- ✅ Docker Compose 全服务编排（app + celery-worker + celery-beat + nginx + 基础设施）
- ✅ Nginx 反向代理（支持 HTTPS 自签名证书 + HTTP 双端口）
- ✅ Docker Secrets 密钥管理（所有敏感信息通过 secrets 文件注入）
- ✅ 密钥生成脚本（`scripts/generate-secrets.sh`）
- ✅ `.dockerignore` 构建上下文优化
- ✅ 国内镜像源加速（Debian apt + pip）

**Docker Compose 服务架构**：
| 服务 | 镜像/构建 | 描述 |
|------|----------|------|
| postgres | pgvector/pgvector:pg16 | PostgreSQL 16 + pgvector |
| redis | redis:7-alpine | 缓存/消息队列 |
| meilisearch | getmeili/meilisearch:v1.8 | 搜索引擎 |
| app | 本地构建 (production) | FastAPI 应用 |
| celery-worker | 本地构建 (production) | Celery Worker |
| celery-beat | 本地构建 (production) | Celery Beat 定时任务 |
| nginx | openresty/openresty:alpine | Nginx 反向代理 |

**Nginx 配置要点**：
- HTTP 80 → 301 重定向到 HTTPS 443
- HTTPS 443 → 代理到 app:8000
- WebSocket 升级支持（`/ws/` 路径）
- 自签名证书自动生成（entrypoint.sh）
- 安全头（X-Frame-Options、X-Content-Type-Options 等）

---

### Phase 5：Sprint 10 — 划词问 AI 与整理到笔记 ✅ 已完成

**时间范围**：2026-07-15 ~ 2026-07-16  
**完成日期**：2026-07-16  
**目标**：笔记编辑器 AI 交互增强，支持划词问 AI、整理到笔记、edit_note 列级精度

#### 5.1 划词问 AI ✅

**交付内容**：
- ✅ 复用现有 Agent 基础设施（无需新增 API）
- ✅ 笔记关联 Agent 会话（`context_type='note'`, `context_id=note_id`）
- ✅ 同一笔记复用同一 Agent 会话
- ✅ 通过 Agent WebSocket 流式返回回复

**实现方式**：
- 前端在笔记页加载时创建/获取笔记关联的 Agent 会话
- 划词选中文本 + 提示词通过 Agent WebSocket 发送
- AI 回复在 ChatPanel 中流式展示，附带"整理到笔记"按钮

#### 5.2 整理到笔记 ✅

**交付内容**：
- ✅ 整理到笔记 API（`POST /notes/{note_id}/organize-from-chat`）
- ✅ 隐藏 Agent 会话（`visibility='hidden'`，Agent 页可见，笔记侧边栏不可见）
- ✅ Celery 异步整理任务（`organize_from_chat_task`，Agent 驱动）
- ✅ 整理进度 WebSocket（`/ws/organize-from-chat/{task_id}`，Redis Pub/Sub）
- ✅ 整理到笔记 Agent Prompt（`note_organize_agent.py`）
- ✅ 安全控制：只允许 insert 操作，禁止 delete/replace

**数据库变更**：
- `agent_sessions` 表新增 `visibility` 字段（String(20)，默认 "visible"，可选 "hidden"）
- CHECK 约束：`visibility IN ('visible', 'hidden')`
- Alembic 迁移：`20260715_add_agent_visibility.py`

**核心实现**：

**1. Agent 服务升级（`agent_service.py`）**：
- `create_hidden_session()` — 创建隐藏 Agent 会话（`visibility='hidden'`）
- `get_sessions()` 新增可选 `visibility` 参数筛选

**2. Celery 任务（`note_tasks.py`）**：
- `organize_from_chat_task` — 整理到笔记异步任务：
  1. 查询笔记内容（title + content）
  2. 构建 Agent 系统 Prompt（注入笔记全文 + AI 回复内容）
  3. 创建 AgentLoop（`max_steps=3`, `token_budget=8000`）
  4. 工具白名单：`get_note_content` + `edit_note`
  5. Agent 自主决定插入位置，调用 `edit_note` 写入
  6. 通过 Redis Pub/Sub 推送进度

**3. Prompt 模板（`prompts/note_organize_agent.py`）**：
- `NOTE_ORGANIZE_AGENT_SYSTEM_PROMPT` — 整理到笔记 Agent 系统 Prompt
  - 变量：`note_content`（笔记全文）、`content_to_organize`（AI 回复内容）
  - 规则：分析笔记结构 → 判断插入位置 → 调用 edit_note insert → 保持风格一致
- `NOTE_ORGANIZE_USER_PROMPT_WITH_SELECTION` — 有划词原文时的用户消息
- `NOTE_ORGANIZE_USER_PROMPT_WITHOUT_SELECTION` — 无划词原文时的用户消息

**4. WebSocket 进度端点**：
- 路径：`/ws/organize-from-chat/{task_id}`（app 级别路由）
- 认证：JWT token 通过 Query 参数传递
- Redis Pub/Sub 频道：`organize_from_chat_progress:{task_id}`

**进度阶段**：
| 阶段 | 百分比 | 说明 |
|------|--------|------|
| starting | 5% | 任务启动，创建 Agent 会话 |
| reading_note | 15% | Agent 读取笔记内容 |
| analyzing | 30% | Agent 分析回复内容，决定插入位置 |
| editing | 60% | Agent 调用 edit_note 工具写入 |
| complete | 100% | 整理完成 |
| error | - | 整理失败 |

**API 端点**：
```
POST /api/v1/notes/{note_id}/organize-from-chat   # 整理到笔记（从 AI 回复）
```

**WebSocket 端点**：
```
WS /ws/organize-from-chat/{task_id}?token={access_token}   # 整理进度监听
```

**Schema**：
```python
class OrganizeFromChatRequest(BaseModel):
    ai_reply_content: str              # AI 回复的完整内容
    selected_text: str | None = None   # 用户划词选中的文本
    user_prompt: str | None = None     # 用户提示词

class OrganizeFromChatResponse(BaseModel):
    agent_session_id: uuid.UUID        # 隐藏 Agent 会话 ID
    task_id: str                       # Celery 任务 ID
    message: str
```

#### 5.3 edit_note 工具增强（列级精度） ✅

**交付内容**：
- ✅ `edit_note` 工具新增 `start_column` / `end_column` 可选参数
- ✅ 列级 insert：在指定行的指定列位置前插入内容
- ✅ 列级 replace：替换从 (start_line, start_column) 到 (end_line, end_column) 的文本
- ✅ 列级 delete：删除从 (start_line, start_column) 到 (end_line, end_column) 的文本
- ✅ 向后兼容：不传列参数时行为与行级编辑完全一致

**列级操作规则**：
| 操作 | start_column | end_column | 行为 |
|------|-------------|-----------|------|
| insert | 指定 | 忽略 | 在 start_line 的 start_column 位置前插入 |
| replace | 指定 | 指定 | 替换跨行跨列的文本范围 |
| delete | 指定 | 指定 | 删除跨行跨列的文本范围 |
| 行级（向后兼容） | None | None | 整行操作 |

**核心实现**（`note_handlers.py`）：
- `is_column_mode = start_column is not None` 判断操作模式
- 列级 insert：定位到 `lines[start_line - 1]`，在 `start_column` 位置插入
- 列级 replace/delete：支持单行和跨行场景
- 边界处理：列号超限时自动裁剪到行长度

#### 5.4 Markdown 标签跳转（纯前端，后端零改动）

**说明**：支持 `[标签名:具体内容]` 语法，两个同名标签组成一对，点击任一个可跳转到配对位置。纯前端实现，后端无改动。

---

## 四、API 端点清单

### 4.1 认证模块（/api/v1/auth）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| POST | /auth/register | 用户注册 | ✅ |
| POST | /auth/login | 登录 | ✅ |
| POST | /auth/token | OAuth2 表单登录（Swagger UI） | ✅ |
| POST | /auth/refresh | 刷新令牌 | ✅ |
| POST | /auth/logout | 登出 | ✅ |

### 4.2 用户模块（/api/v1/users）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /users/me | 获取当前用户信息 | ✅ |
| PATCH | /users/me | 更新用户信息 | ✅ |
| GET | /users/me/profile | 获取知识画像摘要 | ✅ |

### 4.3 笔记模块（/api/v1/notes）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /notes | 笔记列表（分页+筛选+搜索） | ✅ |
| POST | /notes | 创建笔记 | ✅ |
| POST | /notes/organize | AI 整理笔记（Sprint 9） | ✅ |
| POST | /notes/{id}/organize-from-chat | 整理到笔记（Sprint 10） | ✅ |
| GET | /notes/{id} | 笔记详情 | ✅ |
| PUT | /notes/{id} | 更新笔记 | ✅ |
| DELETE | /notes/{id} | 删除笔记 | ✅ |

### 4.4 标签模块（/api/v1/tags）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /tags | 标签列表 | ✅ |
| POST | /tags | 创建标签 | ✅ |
| POST | /tags/notes/{id}/tags | 添加笔记标签 | ✅ |
| DELETE | /tags/notes/{id}/tags/{tid} | 移除笔记标签 | ✅ |

### 4.5 知识图谱模块（/api/v1/knowledge）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /knowledge/subjects | 获取学科列表 | ✅ |
| GET | /knowledge/nodes | 查询知识节点（分页+筛选） | ✅ |
| POST | /knowledge/nodes | 创建知识节点 | ✅ |
| GET | /knowledge/nodes/{id}/graph | 获取节点图结构 | ✅ |
| GET | /knowledge/nodes/{id}/path | 获取学习路径 | ✅ |
| POST | /knowledge/edges | 创建知识边 | ✅ |

### 4.6 学习引擎模块（/api/v1/learning）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| POST | /learning/routes | 创建学习路线 | ✅ |
| GET | /learning/routes | 路线列表 | ✅ |
| GET | /learning/routes/{id} | 路线详情 | ✅ |
| PATCH | /learning/routes/{id}/steps/{sid}/complete | 完成步骤 | ✅ |
| DELETE | /learning/routes/{id} | 删除路线 | ✅ |
| DELETE | /learning/routes/batch | 批量删除路线 | ✅ |
| POST | /learning/lectures/generate | 生成讲义 | ✅ |
| GET | /learning/lectures/{id} | 获取讲义 | ✅ |

### 4.7 智能答疑模块（/api/v1/learning/qa）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| POST | /learning/qa/sessions | 创建答疑会话 | ✅ |
| GET | /learning/qa/sessions | 会话列表 | ✅ |
| POST | /learning/qa/sessions/{id}/messages | 发送消息（非流式） | ✅ |
| GET | /learning/qa/sessions/{id}/messages | 历史消息 | ✅ |
| POST | /learning/qa/diagnostic-questions | 生成诊断性问题 | ✅ |

### 4.8 复习系统模块（/api/v1/review）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /review/plans | 复习计划列表 | ✅ |
| POST | /review/plans/{id}/complete | 完成复习 | ✅ |
| POST | /review/generate-content | 生成复习内容 | ✅ |
| GET | /review/stats | 复习统计 | ✅ |

### 4.9 搜索模块（/api/v1/search）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| GET | /search/global | 全局搜索（Meilisearch + DB 双通道） | ✅ |
| GET | /search/semantic | 语义搜索 | ✅ |
| POST | /search/reindex | 手动重建索引 | ✅ |

### 4.10 Agent 模块（/api/v1/agent）

| 方法 | 路径 | 描述 | 状态 |
|------|------|------|------|
| POST | /agent/sessions | 创建 Agent 会话 | ✅ |
| GET | /agent/sessions | 会话列表（支持 visibility 筛选，Sprint 10） | ✅ |
| GET | /agent/sessions/{id} | 会话详情 | ✅ |
| POST | /agent/sessions/{id}/close | 关闭会话 | ✅ |
| DELETE | /agent/sessions/{id} | 删除会话 | ✅ |
| POST | /agent/sessions/batch-delete | 批量删除会话 | ✅ |
| POST | /agent/sessions/{id}/messages | 发送消息（HTTP 非流式） | ✅ |
| GET | /agent/sessions/{id}/messages | 获取历史消息 | ✅ |
| POST | /agent/sessions/{id}/cancel | 取消生成 | ✅ |
| GET | /agent/tools | 获取可用工具列表 | ✅ |

### 4.11 WebSocket 端点

| 路径 | 描述 | 状态 |
|------|------|------|
| /ws/lecture-progress/{lecture_id} | 讲义生成进度 | ✅ |
| /ws/route-progress/{route_id} | 学习路线生成进度 | ✅ |
| /ws/qa-stream/{session_id} | 答疑对话流式输出 | ✅ |
| /ws/notifications | 系统通知（复习提醒等） | ✅ |
| /ws/agent/{session_id} | Agent 对话（Phase 3） | ✅ |
| /ws/organize-progress/{task_id} | AI 整理笔记进度（Sprint 9） | ✅ |
| /ws/organize-from-chat/{task_id} | 整理到笔记进度（Sprint 10） | ✅ |

---

## 五、核心算法与实现细节

### 5.1 知识掌握度更新（指数移动平均）

```python
# 正确回答
mastery_score += (1 - mastery_score) * 0.3

# 错误回答
mastery_score *= 0.7
```

### 5.2 SM-2 间隔重复算法

```python
# quality >= 3（回答正确）
if interval == 0:
    interval = 1
elif interval == 1:
    interval = 6
else:
    interval = round(interval * ease_factor)

ease_factor = ease_factor + (0.1 - (5 - quality) * (0.08 + (5 - quality) * 0.02))
ease_factor = max(1.3, ease_factor)

# quality < 3（回答错误）
interval = 1
ease_factor = max(1.3, ease_factor - 0.2)

next_review = now + timedelta(days=interval)
```

### 5.3 学习路线动态调整

**表现不佳**（用时/预估 > 2）：
- 添加补充练习步骤
- 后续步骤顺延

**表现优秀**（用时/预估 < 0.5）：
- 合并后续两个步骤
- 跳过第二个步骤

### 5.4 复习内容自动选择

```python
if mastery_score > 0.7:
    review_type = "flashcard"      # 闪卡
elif mastery_score > 0.4:
    review_type = "quiz"           # 选择题/填空题
else:
    review_type = "explanation"    # 重新讲解
```

---

## 六、数据库模型统计

### 6.1 模型总数：23 个

| 模型 | 表名 | 描述 |
|------|------|------|
| User | users | 用户 |
| UserProfile | user_profiles | 用户画像 |
| PersonalHabitProfile | personal_habit_profiles | 个人习惯画像 |
| RefreshToken | refresh_tokens | 刷新令牌 |
| Note | notes | 笔记 |
| Tag | tags | 标签 |
| NoteTag | note_tags | 笔记标签关联 |
| KnowledgeNode | knowledge_nodes | 知识节点 |
| KnowledgeEdge | knowledge_edges | 知识边 |
| UserKnowledgeMastery | user_knowledge_mastery | 用户知识掌握度 |
| LearningRoute | learning_routes | 学习路线 |
| LearningRouteStep | learning_route_steps | 路线步骤 |
| Lecture | lectures | 讲义 |
| LearningRecord | learning_records | 学习活动记录 |
| QASession | qa_sessions | 答疑会话 |
| QAMessage | qa_messages | 答疑消息 |
| ReviewPlan | review_plans | 复习计划 |
| MemoryIndex | memory_indexes | 记忆索引 |
| MemoryFull | memory_fulls | 记忆完整数据 |
| DocumentChunk | document_chunks | 文档分块 |
| AgentSession | agent_sessions | Agent 会话（Phase 3，Sprint 10 新增 visibility 字段） |
| AgentMessage | agent_messages | Agent 消息（Phase 3） |

### 6.2 数据库迁移

- `5fe73777a71e_initial_schema.py` - 初始 Schema
- `ff551d88ddc7_initial_schema.py` - 初始 Schema（补充）
- `c0826ebcce52_add_note_id_to_lectures.py` - Lecture 新增 note_id 字段（讲义自动关联笔记）
- `20260710_add_agent_tables.py` - 新增 agent_sessions + agent_messages 表（Agent 会话与消息）
- `20260711_change_embedding_dim_to_512.py` - 嵌入维度从 1024 调整为 512
- `20260711_add_note_organize_fields.py` - Note 新增 origin_type + source_note_ids 字段（AI 整理溯源）
- `20260715_add_agent_visibility.py` - AgentSession 新增 visibility 字段（区分普通/隐藏会话，Sprint 10）

---

## 七、异步任务清单

### 7.1 Celery 任务

| 任务 | 描述 | 触发方式 | 签名 | 状态 |
|------|------|----------|------|------|
| generate_learning_route | 异步生成学习路线 | API 调用 | route_id, user_id, topic, goal, available_hours, current_level | ✅ |
| generate_lecture_content_task | 异步生成讲义（Sprint 8 升级） | API 调用 | lecture_id, user_id, node_id, route_id, step_id, custom_instructions | ✅ |
| organize_notes_task | AI 整理笔记（Sprint 9） | API 调用 | user_id_str, notes_data_json, prompt | ✅ |
| organize_from_chat_task | 整理到笔记（Sprint 10） | API 调用 | user_id_str, note_id_str, agent_session_id_str, ai_reply_content, selected_text, user_prompt | ✅ |
| check_review_reminders | 检查到期复习计划 | Celery Beat（每 5 分钟） | - | ✅ |
| generate_lecture_task | 旧版讲义生成 | -（deprecated） | - | ❌ 未注册 |

### 7.2 Celery 配置

- **Broker**：Redis (redis://localhost:6379/1)
- **Backend**：Redis (redis://localhost:6379/2)
- **时区**：Asia/Shanghai
- **任务超时**：300 秒（硬限制），240 秒（软限制）
- **Beat 调度**：每 5 分钟检查复习提醒

---

## 八、WebSocket 连接管理器

### 8.1 ConnectionManager

```python
class ConnectionManager:
    active_connections: dict[str, WebSocket]  # client_id -> websocket
    
    async def connect(websocket, client_id)
    def disconnect(client_id)
    async def send_json(client_id, data)  # 异常安全
    async def broadcast_to_prefix(prefix, data)  # 前缀匹配广播
```

### 8.2 Redis Pub/Sub 跨进程通信（Sprint 8）

讲义生成进度通过 Redis Pub/Sub 实现 Celery worker → FastAPI 跨进程推送：

```python
# 发布（Celery worker 进程，同步 Redis 客户端避免 event loop 冲突）
def publish_lecture_progress(lecture_id: str, data: dict):
    _sync_redis.publish(f"lecture_progress:{lecture_id}", json.dumps(data))

# 订阅（FastAPI WebSocket 进程，async Redis 客户端）
async_redis = redis.Redis.from_url("redis://localhost:6379/3")
pubsub = async_redis.pubsub()
await pubsub.subscribe(f"lecture_progress:{lecture_id}")
```

**进度阶段**：
| 阶段 | 百分比 | 说明 |
|------|--------|------|
| connecting | 0% | WebSocket 连接建立（FastAPI 发送） |
| preparing_context | 10% | 任务启动，查询知识图谱/RAG/网络搜索 |
| generating_content | 30% | 调用 LLM 生成讲义内容 |
| finalizing | 80% | LLM 返回，保存讲义 + 创建关联笔记 |
| complete | 100% | 全部完成，携带 lecture 数据 + note_id |
| error | - | 异常时推送错误消息 |

**设计要点**：
- 发布端使用**同步 Redis**（`redis.Redis`）避免 Celery worker 中的 event loop 冲突。
  Celery `run_async()` 每次任务创建新 loop 并在完成后关闭，`redis.asyncio` 缓存连接会绑定到已关闭的 loop 导致 `RuntimeError: Event loop is closed`。
- 订阅端每次 WebSocket 连接创建**新的 async Redis 客户端**，确保连接绑定到正确的 event loop。

### 8.3 Client ID 命名规范

| 前缀 | 格式 | 描述 |
|------|------|------|
| lecture_ | lecture_{lecture_id} | 讲义生成进度 |
| route_ | route_{route_id} | 学习路线生成进度 |
| qa_ | qa_{session_id} | 答疑流式输出 |
| user_ | user_{user_id} | 用户通知（复习提醒 + Agent 编辑推送） |
| organize_ | organize_progress:{task_id} | AI 整理笔记进度（Sprint 9） |
| organize_chat_ | organize_from_chat_progress:{task_id} | 整理到笔记进度（Sprint 10） |

---

## 九、技术亮点与创新

### 9.1 知识图谱驱动的学习路线生成

1. **Apache AGE 集成**：PostgreSQL 扩展，通过 Cypher 查询语言操作图数据
2. **双数据源**：AGE 图查询失败时自动降级到关系表查询
3. **LLM + 图谱**：结合知识图谱的前置依赖关系和 LLM 的教学经验生成路线
4. **动态调整**：根据学生表现实时调整路线难度

### 9.2 苏格拉底式答疑

1. **引导式教学**：不直接给答案，通过提问引导
2. **上下文感知**：结合知识节点、掌握度、讲义内容构建上下文
3. **流式输出**：WebSocket 逐 token 推送，提升用户体验
4. **实时评估**：LLM 分析学生回答质量，实时更新掌握度

### 9.3 SM-2 间隔重复算法

1. **科学复习**：基于艾宾浩斯遗忘曲线
2. **自适应难度**：根据复习表现动态调整下次复习时间
3. **自动创建**：完成复习后自动创建下一次计划
4. **定时提醒**：Celery Beat 每 5 分钟检查到期复习

### 9.4 全异步架构

1. **FastAPI + SQLAlchemy 2.0 async**：全异步 Web 框架
2. **Celery 异步任务**：耗时操作（LLM 调用）异步处理
3. **WebSocket 实时通信**：流式输出、进度推送、复习提醒、Agent 对话
4. **异步数据库驱动**：asyncpg，高性能 PostgreSQL 驱动

### 9.5 ReAct Agent 智能体（Phase 3 + Sprint 9 增强）

1. **ReAct 模式**：思考 → 工具调用 → 观察 → 重复，最多 6 轮推理
2. **工具注册系统**：14 个工具覆盖笔记/知识图谱/搜索/答疑/复习/记忆
3. **上下文感知**：根据会话关联的笔记/讲义/路线自动注入上下文
4. **安全控制**：token 预算、推理轮数限制、写操作限流、取消令牌
5. **笔记编辑能力**：Agent 可直接编辑笔记（insert/replace/delete），实时推送到前端
6. **苏格拉底答疑**：Agent 内集成苏格拉底式答疑工具
7. **Meilisearch 优先**：搜索工具全面优先 Meilisearch 索引，回退 PostgreSQL

### 9.6 全局搜索增强（Phase 3）

1. **4 索引覆盖**：Meilisearch 索引笔记/讲义/知识节点/学习路线
2. **双通道搜索**：Meilisearch 全文搜索 + 数据库 ILIKE 模糊搜索兜底
3. **启动自动同步**：应用启动时全量同步 DB 数据到搜索引擎
4. **CRUD 实时同步**：笔记创建/更新/删除时自动同步索引

### 9.7 AI 整理笔记与生产部署（Sprint 9）

1. **AI 笔记整理**：支持多篇笔记合并/摘要/对比/扩展，LLM 驱动，异步处理
2. **整理溯源**：成果笔记记录 origin_type 和 source_note_ids，可追溯来源
3. **配置安全**：_read_secret() 统一密钥管理，支持 Docker secrets / 本地 secrets / 环境变量
4. **生产部署**：Docker 多阶段构建、非 root 用户、Nginx HTTPS 反向代理、全服务编排

### 9.8 划词问 AI 与整理到笔记（Sprint 10）

1. **划词问 AI**：复用 Agent 基础设施，笔记关联会话，流式回复
2. **整理到笔记**：隐藏 Agent 会话自主决定插入位置，Agent 驱动异步整理
3. **edit_note 列级精度**：支持 start_column/end_column 参数，向后兼容行级编辑
4. **会话可见性控制**：visibility 字段区分普通/隐藏会话，Agent 页可见但笔记侧边栏不可见

---

## 十、已知问题与技术债

### 10.1 当前问题

1. **测试覆盖不足**：`tests/` 目录仅有 `conftest.py`，缺少单元测试和集成测试
2. **Apache AGE 依赖**：部分环境可能无法安装 AGE 扩展，已做降级处理
3. **LLM 单模型**：当前仅支持 DeepSeek，未实现多模型路由
4. **Celery worker 需手动重启**：修改 task 函数签名后需手动重启 worker（FastAPI `--reload` 不覆盖 Celery）
5. **知识图谱数据缺失**：`knowledge_nodes` 表为空，需要导入初始知识图谱数据才能发挥知识图谱搜索能力

### 10.2 技术债

1. **Celery 任务中的数据库引擎初始化**：每个任务内部重新创建引擎，存在资源浪费；`worker_process_init` 和任务内各创建一次引擎
2. **WebSocket 连接管理器**：内存存储，不支持多实例部署
3. **RAG 管道**：已在讲义生成中集成，但未在答疑/复习中深度使用
4. **`lecture_tasks.py` 冗余**：旧版讲义生成任务未注册但保留在代码库中，可清理

---

## 十一、后续规划

### Phase 3：Agent 智能体 ✅ 已完成

**完成日期**：2026-07-11  
**实现内容**：ReAct 模式 Agent、12 工具调用、多轮对话、上下文感知、全局搜索增强、数据级联修复

### Sprint 9：AI 整理笔记 + 生产部署 ✅ 已完成

**完成日期**：2026-07-14  
**实现内容**：AI 笔记整理（14 工具）、配置安全加固、Docker 生产环境部署

### Sprint 10：划词问 AI 与整理到笔记 ✅ 已完成

**完成日期**：2026-07-16  
**实现内容**：划词问 AI、整理到笔记（隐藏 Agent 会话）、edit_note 列级精度、Markdown 标签跳转（纯前端）

### 其他优化方向

1. **测试覆盖**：补充单元测试、集成测试、端到端测试
2. **性能优化**：数据库查询优化、缓存策略、WebSocket 连接池
3. **多模型支持**：接入更多 LLM（Qwen、Claude、GPT-4）
4. **RAG 深度集成**：在答疑、复习中集成 RAG 检索
5. **知识图谱数据导入**：导入初始知识图谱数据，发挥知识图谱搜索能力
6. **监控与日志**：完善日志系统、接入 APM（应用性能监控）
7. **部署方案**：Kubernetes 编排、CI/CD 流水线

---

## 十二、快速启动

### 12.1 环境准备

```bash
# 启动基础设施
cd backend
docker-compose up -d

# 创建虚拟环境
python3 -m venv .venv
source .venv/bin/activate

# 安装依赖
pip install -e .

# 数据库迁移
alembic upgrade head

# 启动 Celery Worker
celery -A app.tasks.celery_app worker --loglevel=info

# 启动 Celery Beat
celery -A app.tasks.celery_app beat --loglevel=info

# 启动 FastAPI
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 12.2 访问服务

- **API 文档**：http://localhost:8000/docs
- **健康检查**：http://localhost:8000/health

---

## 十三、总结

青云智学后端系统已完成 Phase 1 ~ Phase 3、Sprint 9 和 Sprint 10 的全部开发任务，实现了：

✅ **完整的项目基础设施**：Docker 环境、数据库迁移、代码规范  
✅ **用户认证与笔记管理**：JWT 认证、笔记 CRUD、全文搜索、标签系统  
✅ **AI 核心链路**：LLM 集成、RAG 基础、异步任务、WebSocket 流式输出  
✅ **用户记忆与画像**：双层记忆、习惯画像、知识画像  
✅ **知识图谱驱动的学习路线**：Apache AGE、LLM 生成、动态调整  
✅ **苏格拉底式智能答疑**：引导式教学、流式输出、实时更新掌握度  
✅ **SM-2 间隔重复复习**：科学复习、自动创建计划、定时提醒  
✅ **讲义增强与笔记联动**：自动创建笔记、RAG+搜索增强 Prompt、Redis Pub/Sub 跨进程通信  
✅ **Agent 智能体**：ReAct 模式、14 工具调用、多轮对话、上下文感知、取消生成、笔记编辑  
✅ **全局搜索增强**：4 索引 Meilisearch、双通道搜索、启动自动同步、CRUD 实时同步  
✅ **数据级联修复**：笔记/路线删除时级联清理关联讲义和搜索索引  
✅ **AI 整理笔记**：多篇笔记合并/摘要/对比/扩展，LLM 驱动，异步处理，整理溯源  
✅ **生产环境部署**：Docker 多阶段构建、非 root 用户、Nginx HTTPS、Docker Secrets、全服务编排  
✅ **划词问 AI 与整理到笔记**：笔记关联 Agent 会话、隐藏会话、Agent 驱动整理、edit_note 列级精度  

**总计**：
- API 端点：50+ 个
- WebSocket 端点：7 个
- ORM 模型：23 个
- Celery 任务：5 个（+1 deprecated）
- Prompt 模板：10 个
- 数据库迁移：7 个
- Agent 工具：14 个
- Meilisearch 索引：4 个

系统已具备完整的学习辅助功能、AI 智能体能力、笔记编辑器 AI 交互和生产环境部署能力，可进入后续优化阶段。
